#include <stdio.h>

/**
 * main - Prints uppercase and lowercase alphabet.
 *
 * Return: Always 0 (Success)
 */
int main(void)
{
char alph;

	for (alph = 'a'; alph <= 'z'; alph++)
		putchar(alph);

	for (alph = 'A'; alph <= 'Z'; letter++)
		putchar(alph);

	putchar('\n');
	return (0);
}
